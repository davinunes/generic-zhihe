# Configuração do CLAT (464XLAT) para modem UFI003
# Converte IPv4 clients -> IPv6-only Vivo (NAT64)

# Prefixo NAT64 (well-known) da Vivo
plat-prefix=64:ff9b::/96

# Interface WAN (4G)
plat-dev=wwan0

# IPv4 local do CLAT (default: 192.0.0.1)
clat-v4-addr=192.0.0.1

